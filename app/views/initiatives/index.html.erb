<h1>Actividad</h1>
<p class="claim">Qué ocurre en el Parlamento Vasco</p>
<%= render :partial => 'activity/activity_menu' %>

<!-- tab_cont -->
<div class="tab_cont">

  <h2>Asuntos</h2>

  <% if !@initiatives.blank? %>  
    <ul>
      <% for ini in @initiatives %>
        <li><%= link_to ini.num_exp, initiative_path(ini) %> | <%= ini.title %></li>
      <% end %>
    </ul>
  <% end %>
</div>